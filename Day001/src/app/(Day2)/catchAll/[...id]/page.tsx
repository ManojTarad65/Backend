import React from 'react'

const page = () => {
  return (
    <div className='h-screen flex items-center justify-center bg-red-400 font-bold text-2xl'>We are using catch all router , so there is no error when we enter any random url</div>
  )
}

export default page